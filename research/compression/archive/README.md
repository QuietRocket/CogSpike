# archive/ — superseded source notes

These are the **original two notes**, frozen and kept for provenance:

| file | what it was |
|---|---|
| `spiking_entropy_coder.typ` | Note I — the coder (spike-time = cross-entropy) |
| `learning_to_be_unsurprised.typ` | Note II — the local plasticity rule |

They are **superseded by** [`../unified_spiking_compression.typ`](../unified_spiking_compression.typ),
the single canonical paper, which merges both, applies every confirmed critique
fix in-text, and folds in a critical self-evaluation.

Do not edit these. Note that relative paths inside them (e.g. references to
`validate.py`, `spiking_entropy_coder.pctl`) point at the **parent** directory,
so they no longer compile in place without adjustment — the validators and the
property templates live one level up, where the canonical paper uses them.
